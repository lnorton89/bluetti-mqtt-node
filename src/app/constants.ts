/** Minimum settle delay in ms after writing the pack selector. */
export const MIN_PACK_SWITCH_DELAY_MS = 500;

/** Busy penalty multiplier applied to the full polling interval. */
export const FULL_INTERVAL_BUSY_PENALTY_MULTIPLIER = 2;

/** Recovery step multiplier applied to the full polling interval. */
export const FULL_INTERVAL_RECOVERY_MULTIPLIER = 2;
